---
interface Props {
  colorClass?: string;
  dataIndex?: string;
}

const { colorClass, dataIndex } = Astro.props;
---
<button
  class:list={["w-4 h-4 rounded-full hover:scale-120 cursor-pointer", colorClass]}
  data-index={dataIndex}
  aria-label="Cambiar tema"
></button>